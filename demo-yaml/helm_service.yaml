kind: Service     
apiVersion: v1     
metadata:
  name: {{ .Values.service_name }}
spec:
  selector:
    app: music    
  type: {{ .Values.service_type }}
  ports:         
  - name: http   
    protocol: TCP
    port:  {{ .Values.port }}
    targetPort: {{ .Values.targetport }}